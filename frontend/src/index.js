import React from "react";
import ReactDOM from "react-dom/client";

// Components

import App from "./App";

// Renderer

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// Data fetching
// useEffect and event Listners
// useReducer
// costume hooks
// PorpTypes

//Editing the user
//excensive UI
